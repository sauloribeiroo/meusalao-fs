# Banco de dados (PostgreSQL) — local via Docker ou serviço gerenciado (Neon, Supabase, Railway)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/meusalao?schema=public"

# Auth.js — gere com: npx auth secret
AUTH_SECRET=""

# Login social com Google (opcional no ambiente local; o botão só aparece se estiver configurado)
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""
